<nav class="navbar navbar-dark bg-dark">
  <div class="container">
    <div class="d-flex">
      <button class="btn navbar-brand btn-lg" type="button" onclick="location.href='/';">
        <%= image_tag("navbar/logo", alt: "Logo", style: "width: 50px; height: 50px;") %> KariPic
      </button>
      <a class="navbar-brand ms-2" href="/posts">
        <button type="button" class="btn btn-outline-light btn-lg">Publicaciones</button>
      </a>
    </div>

    <div class="navbar-collapse-container">
      <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse"
        data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
        aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDarkDropdownMenuLink" role="button"
              data-bs-toggle="dropdown" aria-expanded="false">
              <% if user_signed_in? %>
              <i class="fa-solid fa-user"></i> Bienvenido/a <%= current_user.email %>
              <% else %>
              <i class="fa-solid fa-user-secret"></i> Inicia sesión
              <% end %>
            </a>
            <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDarkDropdownMenuLink">
              <% if user_signed_in? %>
              <li><%= button_to 'Cerrar sesión', destroy_user_session_path, class: 'dropdown-item', method: :delete %></li>
              <li><%= link_to 'Ver avatar', avatar_path(current_user.id), class: 'dropdown-item' %></li>
              <% else %>
              <li><%= link_to 'Iniciar sesión', new_user_session_path, class: 'dropdown-item' %></li>
              <li><hr class="dropdown-divider" style="border-top: 1px solid #fff;"></li>
              <li><%= link_to 'Registro', new_user_registration_path, class: 'dropdown-item' %></li>
              <% end %>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>








